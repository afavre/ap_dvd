<?php

/**
 * Base class that represents a row from the 'utilisateur' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * Mon Mar 19 16:27:22 2012
 *
 * @package    lib.model.om
 */
abstract class BaseUtilisateur extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        UtilisateurPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the nom field.
	 * @var        string
	 */
	protected $nom;

	/**
	 * The value for the prenom field.
	 * @var        string
	 */
	protected $prenom;

	/**
	 * The value for the nom_prenom_clean field.
	 * @var        string
	 */
	protected $nom_prenom_clean;

	/**
	 * The value for the image field.
	 * @var        string
	 */
	protected $image;

	/**
	 * The value for the login field.
	 * @var        string
	 */
	protected $login;

	/**
	 * The value for the pass field.
	 * @var        string
	 */
	protected $pass;

	/**
	 * The value for the date_naissance field.
	 * @var        string
	 */
	protected $date_naissance;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * @var        array Noteserie[] Collection to store aggregation of Noteserie objects.
	 */
	protected $collNoteseries;

	/**
	 * @var        Criteria The criteria used to select the current contents of collNoteseries.
	 */
	private $lastNoteserieCriteria = null;

	/**
	 * @var        array Notevideo[] Collection to store aggregation of Notevideo objects.
	 */
	protected $collNotevideos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collNotevideos.
	 */
	private $lastNotevideoCriteria = null;

	/**
	 * @var        array Commentaireacteur[] Collection to store aggregation of Commentaireacteur objects.
	 */
	protected $collCommentaireacteurs;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCommentaireacteurs.
	 */
	private $lastCommentaireacteurCriteria = null;

	/**
	 * @var        array Commentairerealisateur[] Collection to store aggregation of Commentairerealisateur objects.
	 */
	protected $collCommentairerealisateurs;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCommentairerealisateurs.
	 */
	private $lastCommentairerealisateurCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'UtilisateurPeer';

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [nom] column value.
	 * 
	 * @return     string
	 */
	public function getNom()
	{
		return $this->nom;
	}

	/**
	 * Get the [prenom] column value.
	 * 
	 * @return     string
	 */
	public function getPrenom()
	{
		return $this->prenom;
	}

	/**
	 * Get the [nom_prenom_clean] column value.
	 * 
	 * @return     string
	 */
	public function getNomPrenomClean()
	{
		return $this->nom_prenom_clean;
	}

	/**
	 * Get the [image] column value.
	 * 
	 * @return     string
	 */
	public function getImage()
	{
		return $this->image;
	}

	/**
	 * Get the [login] column value.
	 * 
	 * @return     string
	 */
	public function getLogin()
	{
		return $this->login;
	}

	/**
	 * Get the [pass] column value.
	 * 
	 * @return     string
	 */
	public function getPass()
	{
		return $this->pass;
	}

	/**
	 * Get the [date_naissance] column value.
	 * 
	 * @return     string
	 */
	public function getDateNaissance()
	{
		return $this->date_naissance;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}


		if ($this->updated_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->updated_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Utilisateur The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = UtilisateurPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [nom] column.
	 * 
	 * @param      string $v new value
	 * @return     Utilisateur The current object (for fluent API support)
	 */
	public function setNom($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nom !== $v) {
			$this->nom = $v;
			$this->modifiedColumns[] = UtilisateurPeer::NOM;
		}

		return $this;
	} // setNom()

	/**
	 * Set the value of [prenom] column.
	 * 
	 * @param      string $v new value
	 * @return     Utilisateur The current object (for fluent API support)
	 */
	public function setPrenom($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->prenom !== $v) {
			$this->prenom = $v;
			$this->modifiedColumns[] = UtilisateurPeer::PRENOM;
		}

		return $this;
	} // setPrenom()

	/**
	 * Set the value of [nom_prenom_clean] column.
	 * 
	 * @param      string $v new value
	 * @return     Utilisateur The current object (for fluent API support)
	 */
	public function setNomPrenomClean($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nom_prenom_clean !== $v) {
			$this->nom_prenom_clean = $v;
			$this->modifiedColumns[] = UtilisateurPeer::NOM_PRENOM_CLEAN;
		}

		return $this;
	} // setNomPrenomClean()

	/**
	 * Set the value of [image] column.
	 * 
	 * @param      string $v new value
	 * @return     Utilisateur The current object (for fluent API support)
	 */
	public function setImage($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->image !== $v) {
			$this->image = $v;
			$this->modifiedColumns[] = UtilisateurPeer::IMAGE;
		}

		return $this;
	} // setImage()

	/**
	 * Set the value of [login] column.
	 * 
	 * @param      string $v new value
	 * @return     Utilisateur The current object (for fluent API support)
	 */
	public function setLogin($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->login !== $v) {
			$this->login = $v;
			$this->modifiedColumns[] = UtilisateurPeer::LOGIN;
		}

		return $this;
	} // setLogin()

	/**
	 * Set the value of [pass] column.
	 * 
	 * @param      string $v new value
	 * @return     Utilisateur The current object (for fluent API support)
	 */
	public function setPass($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->pass !== $v) {
			$this->pass = $v;
			$this->modifiedColumns[] = UtilisateurPeer::PASS;
		}

		return $this;
	} // setPass()

	/**
	 * Set the value of [date_naissance] column.
	 * 
	 * @param      string $v new value
	 * @return     Utilisateur The current object (for fluent API support)
	 */
	public function setDateNaissance($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->date_naissance !== $v) {
			$this->date_naissance = $v;
			$this->modifiedColumns[] = UtilisateurPeer::DATE_NAISSANCE;
		}

		return $this;
	} // setDateNaissance()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Utilisateur The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = UtilisateurPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Utilisateur The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = UtilisateurPeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->nom = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->prenom = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->nom_prenom_clean = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->image = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->login = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->pass = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->date_naissance = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->created_at = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->updated_at = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 10; // 10 = UtilisateurPeer::NUM_COLUMNS - UtilisateurPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Utilisateur object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(UtilisateurPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = UtilisateurPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collNoteseries = null;
			$this->lastNoteserieCriteria = null;

			$this->collNotevideos = null;
			$this->lastNotevideoCriteria = null;

			$this->collCommentaireacteurs = null;
			$this->lastCommentaireacteurCriteria = null;

			$this->collCommentairerealisateurs = null;
			$this->lastCommentairerealisateurCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(UtilisateurPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseUtilisateur:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				UtilisateurPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseUtilisateur:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(UtilisateurPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseUtilisateur:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			// symfony_timestampable behavior
			if ($this->isModified() && !$this->isColumnModified(UtilisateurPeer::UPDATED_AT))
			{
			  $this->setUpdatedAt(time());
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
				// symfony_timestampable behavior
				if (!$this->isColumnModified(UtilisateurPeer::CREATED_AT))
				{
				  $this->setCreatedAt(time());
				}

			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseUtilisateur:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				UtilisateurPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			if ($this->isNew() ) {
				$this->modifiedColumns[] = UtilisateurPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = UtilisateurPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += UtilisateurPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collNoteseries !== null) {
				foreach ($this->collNoteseries as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collNotevideos !== null) {
				foreach ($this->collNotevideos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCommentaireacteurs !== null) {
				foreach ($this->collCommentaireacteurs as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCommentairerealisateurs !== null) {
				foreach ($this->collCommentairerealisateurs as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = UtilisateurPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collNoteseries !== null) {
					foreach ($this->collNoteseries as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collNotevideos !== null) {
					foreach ($this->collNotevideos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCommentaireacteurs !== null) {
					foreach ($this->collCommentaireacteurs as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCommentairerealisateurs !== null) {
					foreach ($this->collCommentairerealisateurs as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = UtilisateurPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getNom();
				break;
			case 2:
				return $this->getPrenom();
				break;
			case 3:
				return $this->getNomPrenomClean();
				break;
			case 4:
				return $this->getImage();
				break;
			case 5:
				return $this->getLogin();
				break;
			case 6:
				return $this->getPass();
				break;
			case 7:
				return $this->getDateNaissance();
				break;
			case 8:
				return $this->getCreatedAt();
				break;
			case 9:
				return $this->getUpdatedAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = UtilisateurPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getNom(),
			$keys[2] => $this->getPrenom(),
			$keys[3] => $this->getNomPrenomClean(),
			$keys[4] => $this->getImage(),
			$keys[5] => $this->getLogin(),
			$keys[6] => $this->getPass(),
			$keys[7] => $this->getDateNaissance(),
			$keys[8] => $this->getCreatedAt(),
			$keys[9] => $this->getUpdatedAt(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = UtilisateurPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setNom($value);
				break;
			case 2:
				$this->setPrenom($value);
				break;
			case 3:
				$this->setNomPrenomClean($value);
				break;
			case 4:
				$this->setImage($value);
				break;
			case 5:
				$this->setLogin($value);
				break;
			case 6:
				$this->setPass($value);
				break;
			case 7:
				$this->setDateNaissance($value);
				break;
			case 8:
				$this->setCreatedAt($value);
				break;
			case 9:
				$this->setUpdatedAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = UtilisateurPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setNom($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setPrenom($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setNomPrenomClean($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setImage($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setLogin($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setPass($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setDateNaissance($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setCreatedAt($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setUpdatedAt($arr[$keys[9]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);

		if ($this->isColumnModified(UtilisateurPeer::ID)) $criteria->add(UtilisateurPeer::ID, $this->id);
		if ($this->isColumnModified(UtilisateurPeer::NOM)) $criteria->add(UtilisateurPeer::NOM, $this->nom);
		if ($this->isColumnModified(UtilisateurPeer::PRENOM)) $criteria->add(UtilisateurPeer::PRENOM, $this->prenom);
		if ($this->isColumnModified(UtilisateurPeer::NOM_PRENOM_CLEAN)) $criteria->add(UtilisateurPeer::NOM_PRENOM_CLEAN, $this->nom_prenom_clean);
		if ($this->isColumnModified(UtilisateurPeer::IMAGE)) $criteria->add(UtilisateurPeer::IMAGE, $this->image);
		if ($this->isColumnModified(UtilisateurPeer::LOGIN)) $criteria->add(UtilisateurPeer::LOGIN, $this->login);
		if ($this->isColumnModified(UtilisateurPeer::PASS)) $criteria->add(UtilisateurPeer::PASS, $this->pass);
		if ($this->isColumnModified(UtilisateurPeer::DATE_NAISSANCE)) $criteria->add(UtilisateurPeer::DATE_NAISSANCE, $this->date_naissance);
		if ($this->isColumnModified(UtilisateurPeer::CREATED_AT)) $criteria->add(UtilisateurPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(UtilisateurPeer::UPDATED_AT)) $criteria->add(UtilisateurPeer::UPDATED_AT, $this->updated_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);

		$criteria->add(UtilisateurPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Utilisateur (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setNom($this->nom);

		$copyObj->setPrenom($this->prenom);

		$copyObj->setNomPrenomClean($this->nom_prenom_clean);

		$copyObj->setImage($this->image);

		$copyObj->setLogin($this->login);

		$copyObj->setPass($this->pass);

		$copyObj->setDateNaissance($this->date_naissance);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getNoteseries() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addNoteserie($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getNotevideos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addNotevideo($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCommentaireacteurs() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCommentaireacteur($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCommentairerealisateurs() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCommentairerealisateur($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Utilisateur Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     UtilisateurPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new UtilisateurPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collNoteseries collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addNoteseries()
	 */
	public function clearNoteseries()
	{
		$this->collNoteseries = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collNoteseries collection (array).
	 *
	 * By default this just sets the collNoteseries collection to an empty array (like clearcollNoteseries());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initNoteseries()
	{
		$this->collNoteseries = array();
	}

	/**
	 * Gets an array of Noteserie objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Utilisateur has previously been saved, it will retrieve
	 * related Noteseries from storage. If this Utilisateur is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Noteserie[]
	 * @throws     PropelException
	 */
	public function getNoteseries($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collNoteseries === null) {
			if ($this->isNew()) {
			   $this->collNoteseries = array();
			} else {

				$criteria->add(NoteseriePeer::UTILISATEUR_ID, $this->id);

				NoteseriePeer::addSelectColumns($criteria);
				$this->collNoteseries = NoteseriePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(NoteseriePeer::UTILISATEUR_ID, $this->id);

				NoteseriePeer::addSelectColumns($criteria);
				if (!isset($this->lastNoteserieCriteria) || !$this->lastNoteserieCriteria->equals($criteria)) {
					$this->collNoteseries = NoteseriePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastNoteserieCriteria = $criteria;
		return $this->collNoteseries;
	}

	/**
	 * Returns the number of related Noteserie objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Noteserie objects.
	 * @throws     PropelException
	 */
	public function countNoteseries(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collNoteseries === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(NoteseriePeer::UTILISATEUR_ID, $this->id);

				$count = NoteseriePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(NoteseriePeer::UTILISATEUR_ID, $this->id);

				if (!isset($this->lastNoteserieCriteria) || !$this->lastNoteserieCriteria->equals($criteria)) {
					$count = NoteseriePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collNoteseries);
				}
			} else {
				$count = count($this->collNoteseries);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Noteserie object to this object
	 * through the Noteserie foreign key attribute.
	 *
	 * @param      Noteserie $l Noteserie
	 * @return     void
	 * @throws     PropelException
	 */
	public function addNoteserie(Noteserie $l)
	{
		if ($this->collNoteseries === null) {
			$this->initNoteseries();
		}
		if (!in_array($l, $this->collNoteseries, true)) { // only add it if the **same** object is not already associated
			array_push($this->collNoteseries, $l);
			$l->setUtilisateur($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Utilisateur is new, it will return
	 * an empty collection; or if this Utilisateur has previously
	 * been saved, it will retrieve related Noteseries from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Utilisateur.
	 */
	public function getNoteseriesJoinSaison($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collNoteseries === null) {
			if ($this->isNew()) {
				$this->collNoteseries = array();
			} else {

				$criteria->add(NoteseriePeer::UTILISATEUR_ID, $this->id);

				$this->collNoteseries = NoteseriePeer::doSelectJoinSaison($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(NoteseriePeer::UTILISATEUR_ID, $this->id);

			if (!isset($this->lastNoteserieCriteria) || !$this->lastNoteserieCriteria->equals($criteria)) {
				$this->collNoteseries = NoteseriePeer::doSelectJoinSaison($criteria, $con, $join_behavior);
			}
		}
		$this->lastNoteserieCriteria = $criteria;

		return $this->collNoteseries;
	}

	/**
	 * Clears out the collNotevideos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addNotevideos()
	 */
	public function clearNotevideos()
	{
		$this->collNotevideos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collNotevideos collection (array).
	 *
	 * By default this just sets the collNotevideos collection to an empty array (like clearcollNotevideos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initNotevideos()
	{
		$this->collNotevideos = array();
	}

	/**
	 * Gets an array of Notevideo objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Utilisateur has previously been saved, it will retrieve
	 * related Notevideos from storage. If this Utilisateur is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Notevideo[]
	 * @throws     PropelException
	 */
	public function getNotevideos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collNotevideos === null) {
			if ($this->isNew()) {
			   $this->collNotevideos = array();
			} else {

				$criteria->add(NotevideoPeer::UTILISATEUR_ID, $this->id);

				NotevideoPeer::addSelectColumns($criteria);
				$this->collNotevideos = NotevideoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(NotevideoPeer::UTILISATEUR_ID, $this->id);

				NotevideoPeer::addSelectColumns($criteria);
				if (!isset($this->lastNotevideoCriteria) || !$this->lastNotevideoCriteria->equals($criteria)) {
					$this->collNotevideos = NotevideoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastNotevideoCriteria = $criteria;
		return $this->collNotevideos;
	}

	/**
	 * Returns the number of related Notevideo objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Notevideo objects.
	 * @throws     PropelException
	 */
	public function countNotevideos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collNotevideos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(NotevideoPeer::UTILISATEUR_ID, $this->id);

				$count = NotevideoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(NotevideoPeer::UTILISATEUR_ID, $this->id);

				if (!isset($this->lastNotevideoCriteria) || !$this->lastNotevideoCriteria->equals($criteria)) {
					$count = NotevideoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collNotevideos);
				}
			} else {
				$count = count($this->collNotevideos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Notevideo object to this object
	 * through the Notevideo foreign key attribute.
	 *
	 * @param      Notevideo $l Notevideo
	 * @return     void
	 * @throws     PropelException
	 */
	public function addNotevideo(Notevideo $l)
	{
		if ($this->collNotevideos === null) {
			$this->initNotevideos();
		}
		if (!in_array($l, $this->collNotevideos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collNotevideos, $l);
			$l->setUtilisateur($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Utilisateur is new, it will return
	 * an empty collection; or if this Utilisateur has previously
	 * been saved, it will retrieve related Notevideos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Utilisateur.
	 */
	public function getNotevideosJoinVideo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collNotevideos === null) {
			if ($this->isNew()) {
				$this->collNotevideos = array();
			} else {

				$criteria->add(NotevideoPeer::UTILISATEUR_ID, $this->id);

				$this->collNotevideos = NotevideoPeer::doSelectJoinVideo($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(NotevideoPeer::UTILISATEUR_ID, $this->id);

			if (!isset($this->lastNotevideoCriteria) || !$this->lastNotevideoCriteria->equals($criteria)) {
				$this->collNotevideos = NotevideoPeer::doSelectJoinVideo($criteria, $con, $join_behavior);
			}
		}
		$this->lastNotevideoCriteria = $criteria;

		return $this->collNotevideos;
	}

	/**
	 * Clears out the collCommentaireacteurs collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCommentaireacteurs()
	 */
	public function clearCommentaireacteurs()
	{
		$this->collCommentaireacteurs = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCommentaireacteurs collection (array).
	 *
	 * By default this just sets the collCommentaireacteurs collection to an empty array (like clearcollCommentaireacteurs());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCommentaireacteurs()
	{
		$this->collCommentaireacteurs = array();
	}

	/**
	 * Gets an array of Commentaireacteur objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Utilisateur has previously been saved, it will retrieve
	 * related Commentaireacteurs from storage. If this Utilisateur is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Commentaireacteur[]
	 * @throws     PropelException
	 */
	public function getCommentaireacteurs($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCommentaireacteurs === null) {
			if ($this->isNew()) {
			   $this->collCommentaireacteurs = array();
			} else {

				$criteria->add(CommentaireacteurPeer::UTILISATEUR_ID, $this->id);

				CommentaireacteurPeer::addSelectColumns($criteria);
				$this->collCommentaireacteurs = CommentaireacteurPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CommentaireacteurPeer::UTILISATEUR_ID, $this->id);

				CommentaireacteurPeer::addSelectColumns($criteria);
				if (!isset($this->lastCommentaireacteurCriteria) || !$this->lastCommentaireacteurCriteria->equals($criteria)) {
					$this->collCommentaireacteurs = CommentaireacteurPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCommentaireacteurCriteria = $criteria;
		return $this->collCommentaireacteurs;
	}

	/**
	 * Returns the number of related Commentaireacteur objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Commentaireacteur objects.
	 * @throws     PropelException
	 */
	public function countCommentaireacteurs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCommentaireacteurs === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CommentaireacteurPeer::UTILISATEUR_ID, $this->id);

				$count = CommentaireacteurPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CommentaireacteurPeer::UTILISATEUR_ID, $this->id);

				if (!isset($this->lastCommentaireacteurCriteria) || !$this->lastCommentaireacteurCriteria->equals($criteria)) {
					$count = CommentaireacteurPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collCommentaireacteurs);
				}
			} else {
				$count = count($this->collCommentaireacteurs);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Commentaireacteur object to this object
	 * through the Commentaireacteur foreign key attribute.
	 *
	 * @param      Commentaireacteur $l Commentaireacteur
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCommentaireacteur(Commentaireacteur $l)
	{
		if ($this->collCommentaireacteurs === null) {
			$this->initCommentaireacteurs();
		}
		if (!in_array($l, $this->collCommentaireacteurs, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCommentaireacteurs, $l);
			$l->setUtilisateur($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Utilisateur is new, it will return
	 * an empty collection; or if this Utilisateur has previously
	 * been saved, it will retrieve related Commentaireacteurs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Utilisateur.
	 */
	public function getCommentaireacteursJoinPersonne($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCommentaireacteurs === null) {
			if ($this->isNew()) {
				$this->collCommentaireacteurs = array();
			} else {

				$criteria->add(CommentaireacteurPeer::UTILISATEUR_ID, $this->id);

				$this->collCommentaireacteurs = CommentaireacteurPeer::doSelectJoinPersonne($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CommentaireacteurPeer::UTILISATEUR_ID, $this->id);

			if (!isset($this->lastCommentaireacteurCriteria) || !$this->lastCommentaireacteurCriteria->equals($criteria)) {
				$this->collCommentaireacteurs = CommentaireacteurPeer::doSelectJoinPersonne($criteria, $con, $join_behavior);
			}
		}
		$this->lastCommentaireacteurCriteria = $criteria;

		return $this->collCommentaireacteurs;
	}

	/**
	 * Clears out the collCommentairerealisateurs collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCommentairerealisateurs()
	 */
	public function clearCommentairerealisateurs()
	{
		$this->collCommentairerealisateurs = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCommentairerealisateurs collection (array).
	 *
	 * By default this just sets the collCommentairerealisateurs collection to an empty array (like clearcollCommentairerealisateurs());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCommentairerealisateurs()
	{
		$this->collCommentairerealisateurs = array();
	}

	/**
	 * Gets an array of Commentairerealisateur objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Utilisateur has previously been saved, it will retrieve
	 * related Commentairerealisateurs from storage. If this Utilisateur is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Commentairerealisateur[]
	 * @throws     PropelException
	 */
	public function getCommentairerealisateurs($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCommentairerealisateurs === null) {
			if ($this->isNew()) {
			   $this->collCommentairerealisateurs = array();
			} else {

				$criteria->add(CommentairerealisateurPeer::UTILISATEUR_ID, $this->id);

				CommentairerealisateurPeer::addSelectColumns($criteria);
				$this->collCommentairerealisateurs = CommentairerealisateurPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CommentairerealisateurPeer::UTILISATEUR_ID, $this->id);

				CommentairerealisateurPeer::addSelectColumns($criteria);
				if (!isset($this->lastCommentairerealisateurCriteria) || !$this->lastCommentairerealisateurCriteria->equals($criteria)) {
					$this->collCommentairerealisateurs = CommentairerealisateurPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCommentairerealisateurCriteria = $criteria;
		return $this->collCommentairerealisateurs;
	}

	/**
	 * Returns the number of related Commentairerealisateur objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Commentairerealisateur objects.
	 * @throws     PropelException
	 */
	public function countCommentairerealisateurs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCommentairerealisateurs === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CommentairerealisateurPeer::UTILISATEUR_ID, $this->id);

				$count = CommentairerealisateurPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CommentairerealisateurPeer::UTILISATEUR_ID, $this->id);

				if (!isset($this->lastCommentairerealisateurCriteria) || !$this->lastCommentairerealisateurCriteria->equals($criteria)) {
					$count = CommentairerealisateurPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collCommentairerealisateurs);
				}
			} else {
				$count = count($this->collCommentairerealisateurs);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Commentairerealisateur object to this object
	 * through the Commentairerealisateur foreign key attribute.
	 *
	 * @param      Commentairerealisateur $l Commentairerealisateur
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCommentairerealisateur(Commentairerealisateur $l)
	{
		if ($this->collCommentairerealisateurs === null) {
			$this->initCommentairerealisateurs();
		}
		if (!in_array($l, $this->collCommentairerealisateurs, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCommentairerealisateurs, $l);
			$l->setUtilisateur($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Utilisateur is new, it will return
	 * an empty collection; or if this Utilisateur has previously
	 * been saved, it will retrieve related Commentairerealisateurs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Utilisateur.
	 */
	public function getCommentairerealisateursJoinPersonne($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UtilisateurPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCommentairerealisateurs === null) {
			if ($this->isNew()) {
				$this->collCommentairerealisateurs = array();
			} else {

				$criteria->add(CommentairerealisateurPeer::UTILISATEUR_ID, $this->id);

				$this->collCommentairerealisateurs = CommentairerealisateurPeer::doSelectJoinPersonne($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CommentairerealisateurPeer::UTILISATEUR_ID, $this->id);

			if (!isset($this->lastCommentairerealisateurCriteria) || !$this->lastCommentairerealisateurCriteria->equals($criteria)) {
				$this->collCommentairerealisateurs = CommentairerealisateurPeer::doSelectJoinPersonne($criteria, $con, $join_behavior);
			}
		}
		$this->lastCommentairerealisateurCriteria = $criteria;

		return $this->collCommentairerealisateurs;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collNoteseries) {
				foreach ((array) $this->collNoteseries as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collNotevideos) {
				foreach ((array) $this->collNotevideos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCommentaireacteurs) {
				foreach ((array) $this->collCommentaireacteurs as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCommentairerealisateurs) {
				foreach ((array) $this->collCommentairerealisateurs as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collNoteseries = null;
		$this->collNotevideos = null;
		$this->collCommentaireacteurs = null;
		$this->collCommentairerealisateurs = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseUtilisateur:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseUtilisateur::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseUtilisateur
